[pytest]
testpaths = tests

# Put the repo root on sys.path so `from engine import ...` resolves.
# Without this, bare `pytest` fails collection while `python -m pytest`
# succeeds -- the latter inserts the cwd, the former does not. CI runs the
# bare form, and so does anyone following the README.
pythonpath = .
